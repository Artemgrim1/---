services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: arbat
      POSTGRES_PASSWORD: arbat
      POSTGRES_DB: arbat
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
